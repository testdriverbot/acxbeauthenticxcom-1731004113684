version: 4.1.8
steps:
  - prompt: Focus the Google Chrome window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: >-
      Assert that the logo is visible on the webpage.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the logo is visible on the webpage
